# Security policy

Please do not publish suspected vulnerabilities in a public issue. Use the
repository's private vulnerability reporting feature or contact the repository
owner through the GitHub organization profile.

Only the latest release receives security fixes. Never include CMR24 keys,
proxy secrets, confirmation secrets, request payloads containing personal data,
or `.env` files in reports.
